Every panel in the product. Border-defined, never shadow-defined.

```jsx
<Card padding="24px">…</Card>
<Card sunken padding="12px">…</Card>
```

Notes: don't nest a Card inside a Card at the same tone — switch the inner one to `sunken`, or drop the border and use a hairline divider instead.
