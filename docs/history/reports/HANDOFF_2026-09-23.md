# Handoff, 2026-09-23: Batch 23 WP-0 planned, rulings in, nothing in flight

Written for the next agent, arriving cold. It supersedes
`docs/history/reports/HANDOFF_2026-09-20.md` as the entry point. That file
still owns the environment, the gates and why they refuse, and the
schema-migration trap, so read its sections 3-5. It is not repeated here.

## 1. Where things stand

**Batch 23 is active on `feat/batch23-wp0-hygiene`,** in the linked worktree
`C:/Users/peter/.config/superpowers/worktrees/ScrobbleScope/batch-21/impeccable-init`.
The branch was cut from `test`. HEAD is the commit that adds this file.
Nothing is pushed, and every commit below awaits owner review.

**WP-0 is next,** and the owner widened it into three parts
(`BATCH23_DEFINITION.md` WP-0):

- **Part A:** the behaviour-neutral extractions. The shared loop protocol is
  done; three extractions remain.
- **Part B:** reconciling what earlier batches left open.
- **Part C:** clearing every finding open at P0 or P1, a fixed set of 38 IDs.

The owner answered all seventeen planning questions (Q0-Q16) on 2026-09-23.
Q0's outcome still needs one check (section 6). No task is half-done, and the
tracked tree is clean.

**Gates at the last commit:**
- `pytest -q`: 1735 passed, with the untracked mutation-runner tests
  excluded;
- `doc_state_sync.py --check`: exit 0, with only the expected root-definition
  warning;
- `pre-commit run --all-files`: all ten hooks passed.

## 2. Read these, in this order

1. `AGENTS.md` Session Bootstrap, then its steps. Run the worktree guard with
   `--base-ref origin/test`, because the branch was cut from `test`
   (`HANDOFF_PROMPT.md` "Bootstrap edge cases").
2. `PLAYBOOK.md` Section 3, "Next action". It owns the order below.
3. `BATCH23_DEFINITION.md` WP-0, including its logging rule.
4. `docs/superpowers/plans/2026-09-23-batch23-wp0-reconcile-and-clear.md`:
   the owner questions with their answers, one disposition per finding, and
   Tasks 1-10.
5. `docs/superpowers/plans/2026-09-21-batch23-wp0-foundation.md`, for Part A
   (Task 2) and Part B's control-plane tasks (Tasks 4-10).

## 3. What landed on 2026-09-23

| Commits | What |
| --- | --- |
| `ad2d078`..`54ab72b` | The worker run-coroutine wrapper plan, all four tasks. `worker.run_coroutine_in_new_loop` is shared by both background entry points, and there are six new tests (1729 to 1735). It ran by subagent-driven development: one fix round on Task 4, and the whole-branch review passed with no findings. |
| `167e650` | The definition amendment: WP-0 in three parts, the logging ruling, and the batch acceptance amended for Part C. |
| `15bbccc` | The reconcile-and-clear plan, written from a four-way triage of all 38 findings. |
| `f84d85e` | The owner's answers to Q0-Q16. |

## 4. Next, in order

1. **Part A:** Task 2 of the foundation plan, the three extractions. Run it
   by subagent-driven development, one implementer at a time. The work shares
   `scrobblescope/orchestrator/__init__.py`, `scrobblescope/heatmap.py` and
   one log entry, so parallel implementers would collide. No existing test
   may change.
2. **The reconcile plan, Tasks 1-10:**
   - Stage 1 closes the stale records.
   - Stage 2 fixes F-SWE-6, F-B22-7 (three tasks), F-SWE-5, F-B21-6 and
     F-LOAD-1.
   - Stage 3 writes the rulings into the findings.
   Stage 2 is written for the answers the owner gave, so run it as written.
3. **The foundation plan, Tasks 4-10:** the control-plane work, each change
   to a check accepted only on a live probe.
4. **Three follow-on plans, to be written:** control-plane, frontend, and
   test-infrastructure plus dependencies. The reconcile plan's "After this
   plan" section scopes each one.
5. **WP-0 close-out:** one tagged `(Batch 23 WP-0)` Section 4 entry, once all
   three parts meet their acceptance.

## 5. Rules specific to this stretch

- **Log every WP-0 commit untagged,** directly after the current-batch end
  marker, with no `WP-<digit>` in the heading. docsync reads a work package
  as complete on its first tagged heading (`scripts/docsync/parser.py`
  `_collect_wp_numbers`), so a tagged entry would name WP-1 as next while
  WP-0 is still open. That gap is to be filed as a finding in the reconcile
  plan's Task 2.
- **Leave the "WP-0 is next" wording intact** in PLAYBOOK Section 3 and on
  the definition's Status line. DOC007 reads both.
- **Part A never edits a test.** Part C may edit one only where the finding
  being fixed requires it, naming every changed assertion in the commit body.
  Never mix the two parts in one commit.
- **Subagents run on Sonnet** (owner cost policy).
- **Commits carry no `Co-authored-by` or other attribution line.**
- **Tasks that touch the docsync control plane** use
  `SKIP=doc-state-sync-check`, never `--no-verify`.

## 6. Open items

- **The Batch 22 MusicBrainz owner check is still owed.**
  - The owner ran twice on 2026-09-23. The first run had Postgres down, which
    skips release checks by design. The second had it up (448 albums, decade
    scope). Neither log shows a MusicBrainz line, and that proves nothing:
    `release_checks._run_release_checks` logs nothing on a successful run,
    only when it skips, stops or fails.
  - The primary checkout (`C:/Users/peter/Python Projects/ScrobbleScope`,
    branch `codex/impeccable-init`) has no `MUSICBRAINZ_CONTACT` in its
    `.env`, and its orchestrator code has no `enqueue_release_check`. This
    worktree's `.env` does set the contact.
  - To settle it, read the results page's release-check disclosure, or
    `GET /api/release_checks?job_id=<id>`, or run
    `select count(*) from original_release_cache` in `ss-postgres`.
  - Record the outcome as Q0's answer in the reconcile plan.
  - Widen the test-infrastructure plan's log-line item to a start line and a
    finish line with counts.
- **The Spotify credential restore is done.** The owner's first run logged
  142 of 146 lookups answered by Spotify. The reconcile plan's Task 1 Step 5
  records it.
- **Two findings stay outside the fixed set but are being fixed:** F-SWE-6 and
  F-B22-7 are P2, and the owner added them through Q1 and Q2.

## 7. Artifacts outside Git

These are git-ignored and untracked. Read them, but never stage them, and
never cite them in backticks in a live document: DOC001 requires a tracked
path.

- **The triage evidence** behind the reconcile plan, one report per cluster:
  .superpowers/sdd/2026-09-23-batch23-wp0-reconcile-and-clear/triage-A-stale-records.md,
  -B-frontend, -C-backend and -D-control-plane. The follow-on plans are
  written from these.
- **A reusable subagent-driven kit:**
  .superpowers/sdd/2026-09-21-worker-run-coroutine-wrapper/ holds
  constraints.md (rulings R1-R11 plus a Lessons log), the implementer and
  reviewer instruction files, the briefs, the reports and the ledger. Copy
  and adapt it for the next plan.
- **The foundation plan's ledger is stale,** because it predates the batch
  opening:
  .superpowers/sdd/2026-09-21-batch23-wp0-foundation/progress.md. Trust
  `git log`.
- **scratch/ and the other untracked files at the root belong to other agents
  or the owner.** That includes the mutation runner, `progress_copy.md` and
  the review HTML files. Leave them alone.

## 8. Traps hit this session

- **A newly written plan cited in backticks fails DOC001 until staged.**
  `git add` it before running pre-commit or `--check`.
- **The pre-commit `worktree-alignment` hook prints `ERROR WT005` against
  `origin/main` and still passes.** It is advisory, and expected for a branch
  cut from `test`.
- **Each untagged entry makes `--fix` rotate the oldest non-current entry**
  into `docs/logarchive/PLAYBOOK_EXECUTION_LOG_ARCHIVE.md`. Stage the archive
  with the commit.
- **Measure the count with the untracked mutation-runner tests excluded**
  (`--ignore=tests/scripts/dev/test_mutation_test.py`). Quote it exactly as
  `` `pytest -q` -- **N passed** ``, with any qualifier after the count.
